//------------------------------------------------------------------------------------------------------------------
#include "radio_buffer.h"
//------------------------------------------------------------------------------------------------------------------
uint8_t radio_buffer_no1[RADIO_BUFFER_MAIN];
static uint8_t radio_buffer_max = 1;
//------------------------------------------------------------------------------------------------------------------
uint8_t* radio_buffer_get(uint8_t idx)
{
    if (idx == 0)
        return radio_buffer_no1;  
    else
        return 0;
}
//------------------------------------------------------------------------------------------------------------------
int radio_buffer_size(uint8_t idx)
{
    if (idx == 0)
        return RADIO_BUFFER_MAIN;
    else
        return 0;
}
//------------------------------------------------------------------------------------------------------------------
int radio_buffer_count(void)
{
    return 1;
}
//------------------------------------------------------------------------------------------------------------------
